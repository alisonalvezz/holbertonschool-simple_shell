#include "simple_shell.h"
#define buffer 50

int main()
{
	/**char *cmd;*/

	display_prompt();

	while (1)
	{

		/**execute_command(cmd);*/
	}

	return (0);
}

void display_prompt(void)
{
        printf("la_shellula ");
        fflush(stdout);
}
